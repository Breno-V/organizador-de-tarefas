services:
  - type: web
    name: organizador
    env: node
    buildCommand: npm install && npm install --prefix frontend && npm install --prefix server && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: APP_URL
        value: https://organizador.onrender.com
